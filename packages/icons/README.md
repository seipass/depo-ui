# @depo-ui/icons

The canonical Depo UI icon catalog and icon metadata types.

Import icons directly when a lower-level integration needs them; product applications can also use the public exports from `@depo-ui/react` where available.
