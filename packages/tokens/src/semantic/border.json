{
  "border": {
    "width": {
      "none": {
        "$type": "dimension",
        "$value": "{border.width.reference-none}",
        "$description": "No border width",
        "$extensions": { "depo-ui": { "tier": "semantic", "status": "stable" } }
      },
      "default": {
        "$type": "dimension",
        "$value": "{border.width.reference-default}",
        "$description": "Default border width",
        "$extensions": { "depo-ui": { "tier": "semantic", "status": "stable" } }
      },
      "strong": {
        "$type": "dimension",
        "$value": "{border.width.reference-strong}",
        "$description": "Strong and focus border width",
        "$extensions": { "depo-ui": { "tier": "semantic", "status": "stable" } }
      }
    }
  }
}
