{
  "$description": "Default dark appearance. Semantic defaults are the dark contract; this file records the active appearance and explicit overlay overrides.",
  "color": {
    "overlay": {
      "scrim": {
        "$type": "color",
        "$value": "{color.neutral.0}",
        "$description": "Dark modal scrim",
        "$extensions": { "depo-ui": { "tier": "theme", "status": "stable", "theme": "dark" } }
      }
    }
  }
}
