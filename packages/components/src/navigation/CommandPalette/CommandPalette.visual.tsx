import type { CommandPaletteProps } from './CommandPalette.types.js';

export function CommandPaletteVisualFixture(_props: CommandPaletteProps) {
  void _props;
  return <div data-visual-fixture="CommandPalette" />;
}
