{
  "name": "@depo-ui/example-consumer-fixture",
  "version": "0.0.0",
  "private": true,
  "description": "Public-facing launch site used to exercise the Depo UI consumer entrypoint",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "cd ../.. && vitest run --config vitest.workspace.ts --passWithNoTests"
  },
  "dependencies": {
    "@depo-ui/react": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "react": "catalog:"
  }
}
